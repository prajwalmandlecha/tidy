tidy init → writes starter config
tidy run → one-shot organize existing files
tidy run --dry-run → preview without touching anything
tidy watch → live watching with debounce
tidy watch --config ./myconfig.yaml --dry-run
